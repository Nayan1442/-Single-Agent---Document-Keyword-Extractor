# 📄 LangGraph Keyword Extractor

This project extracts predefined keywords from `.txt` documents using LangGraph.

## How to Use

1. Place your text file in `input/sample.txt`
2. Run `main.py`
3. See results in `output/result.json`

##  Tech Used

- Python
- LangGraph
- Ollama LLMs (Gemma/Mistral)
- Regex-based context extractor
